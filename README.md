import streamlit as st

# إعداد الصفحة
st.set_page_config(page_title="My Professional Portfolio", page_icon="🚀")

# العنوان الرئيسي
st.title("مرحباً بكم في معرض أعمالي التقني 👋")

# نبذة عني
st.header("حول المشروع")
st.write("""
هذا التطبيق تم بناؤه باستخدام **Python** ومكتبة **Streamlit**. 
لقد قمت بنشره عبر **Render** ليكون متاحاً للجميع على الويب!
""")

# قسم المهارات بشكل تفاعلي
st.subheader("مهاراتي التقنية")
skills = ["Python", "Data Analysis", "Web Deployment", "GitHub"]
st.write(", ".join(skills))

# إضافة تفاعل (زر)
if st.button('اضغط هنا لمشاهدة رسالة خاصة'):
    st.balloons()
    st.success("شكراً لزيارتكم بروفيلي على لينكد إن!")

# روابط التواصل
st.sidebar.title("تواصل معي")
st.sidebar.write("[LinkedIn Profile](رابط_حسابك_هنا)")
